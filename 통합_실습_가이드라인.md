# 🚀 [통합 가이드] Airflow & AWS E2E 데이터 파이프라인 실습

본 가이드는 로컬 데이터의 수집부터 AWS 클라우드 분석까지 이어지는 데이터 엔지니어링의 전 과정을 다룹니다.

---

## 1. Airflow 실습 파이프라인 소개
*   **핵심 컨셉**: 내 컴퓨터의 Airflow를 '리모컨'으로 삼아 AWS의 거대한 인프라를 원격 제어합니다.
*   **파이프라인 흐름**: 
    1.  **Extract**: 로컬 `data/data.json` 확인
    2.  **Load**: Airflow를 통해 S3에 업로드
    3.  **Monitor**: Airflow UI에서 로그 및 성공 여부 확인
    4.  **Catalog**: Glue Crawler로 데이터 지도(Schema) 생성
    5.  **Analyze**: Athena SQL로 데이터 최종 분석

---

## 2. 디렉토리 및 코드 설명
프로젝트의 각 파일은 다음과 같은 역할을 수행합니다.
*   `data/data.json`: 10명의 가상 유저 정보가 담긴 원본 데이터 파일입니다.
*   `dags/user_data_pipeline_manual.py`: 수동 실습용. S3에 데이터를 올리는 단계까지만 수행합니다.
*   `dags/user_data_pipeline_automated.py`: 자동화 실습용. S3 적재 후 Athena 파티션 갱신까지 한 번에 처리합니다.
*   `docker-compose.yml`: Airflow 환경(Webserver, Scheduler, Worker, DB)을 한 번에 띄우는 설정 파일입니다.

---

## 3. AWS 콘솔 접속 및 버킷 생성 (S3/IAM)

### 3.1 S3 버킷 생성
1.  [AWS Console](https://console.aws.amazon.com/) 접속 -> **S3** 검색.
2.  **[버킷 만들기]** 클릭 -> 버킷 이름: `boaz-data-lab-[본인이름]` 입력.
3.  리전: **아시아 태평양(서울) ap-northeast-2** 선택 후 하단 [버킷 만들기] 클릭.

### 3.2 IAM 사용자 발급 (Airflow용)
1.  **IAM** 서비스 검색 -> **사용자** -> **[사용자 생성]** 클릭.
2.  사용자 이름: `airflow-operator` 입력.
3.  권한 설정: **[직접 정책 연결]** 선택 -> `AmazonS3FullAccess`, `AmazonAthenaFullAccess` 검색 후 체크.
4.  생성 완료 후 해당 사용자 클릭 -> **[보안 자격 증명]** -> **[액세스 키 만들기]** -> **[CLI]** 선택.
5.  발급된 **Access Key**와 **Secret Key**를 메모장에 따로 저장하세요.

### 3.3 IAM 역할 생성 (Glue용)
1.  IAM -> **역할(Roles)** -> **[역할 생성]** 클릭.
2.  신뢰할 수 있는 엔터티: **AWS 서비스** -> **Glue** 선택 후 다음.
3.  권한 정책: `AmazonS3FullAccess`, `AWSGlueServiceRole` 두 개 체크 후 다음.
4.  역할 이름: `GlueS3AccessRole` 입력 후 생성.

---

## 4. 도커 실행 및 데이터 S3 적재

### 4.1 Airflow 환경 가동
터미널에서 아래 명령어를 실행합니다.
```bash
docker-compose up -d
docker ps  # 모든 컨테이너가 Up/Healthy 상태인지 확인
```

### 4.2 Airflow Connection 등록
1.  [http://localhost:8080](http://localhost:8080) 접속 (ID/PW: `airflow`).
2.  **Admin -> Connections** -> **[+]** 클릭.
3.  **Conn Id**: `aws_default`, **Conn Type**: `Amazon Web Services` 입력.
4.  Login에 `Access Key`, Password에 `Secret Key` 입력.
5.  Extra에 `{"region_name": "ap-northeast-2"}` 입력 후 **[Save]**.

### 4.3 수동 실습용 DAG 실행
1.  `dags/user_data_pipeline_manual.py`를 열어 `S3_BUCKET_NAME`을 본인 버킷명으로 수정.
2.  Airflow UI에서 `user_data_pipeline_manual` 활성화(ON) 후 **[Trigger DAG]** 클릭.

---

## 5. Airflow 모니터링 및 로그 확인 (데이터 적재 검증)
데이터가 S3로 성공적으로 떠났는지 Airflow UI에서 직접 확인합니다.

1.  **Graph View 확인**: `extract_from_api`와 `upload_to_s3` 태스크가 모두 **진한 초록색(success)**으로 변했는지 확인합니다.
2.  **Log 상세 확인**: `upload_to_s3` 태스크를 클릭하고 **[Logs]** 버튼을 누릅니다.
3.  **물리적 전송 기록 확인**: 로그 내용 중 `INFO - Successfully uploaded to s3://...` 메시지가 있는지 확인합니다. 이 메시지가 있다면 인터넷을 타고 AWS 서울 리전으로 데이터가 무사히 전송된 것입니다.
4.  **S3 콘솔 확인**: 실제 AWS S3 콘솔의 본인 버킷에 `raw/user_data/data.json` 파일이 존재하는지 최종 대조합니다.

---

## 6. Glue Crawler 설정 및 체험

### 6.1 Glue Database 생성
1.  AWS 콘솔 -> **AWS Glue** 검색 -> **Data Catalog -> Databases** 클릭.
2.  **[Add database]** 클릭 -> 이름: `boaz_database` 입력 후 생성.

### 6.2 Glue Crawler 생성 및 실행
1.  **Data Catalog -> Crawlers** 클릭 -> **[Create crawler]** 클릭.
2.  이름: `user_data_crawler` 입력.
3.  Data source: `S3` -> 경로는 `s3://[본인버킷명]/raw/user_data/` 지정.
4.  IAM Role: 앞서 만든 `GlueS3AccessRole` 선택.
5.  Output: Target database를 `boaz_database`로 지정.
6.  생성 완료 후 해당 크롤러 선택 -> **[Run crawler]** 클릭 (1~2분 대기).
7.  상태가 `Ready`가 되고 **Tables created: 1**이 뜨면 성공!

---

## 7. Athena 쿼리문 작성 후 결과 확인
1.  AWS 콘솔 -> **Athena** 검색.
2.  왼쪽 Database에서 `boaz_database` 선택.
3.  생성된 `user_data` 테이블 옆 점 3개 -> **Preview Table** 클릭.
4.  S3의 JSON 데이터가 표 형태로 예쁘게 출력되는지 확인합니다.

---

## 8. 완전 자동화 실습

### 7.1 자동화용 테이블 뼈대 생성 (Athena)
Athena 쿼리 편집기에 아래 내용을 넣고 실행합니다. (버킷명 수정 필수)
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS user_data_auto_lake (
  `id` INT, `name` STRING, `email` STRING, `company_name` STRING, `collected_at` STRING
)
PARTITIONED BY (`dt` STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://[본인버킷이름]/raw/user_data_auto/';
```

### 7.2 자동화 DAG 실행
1.  `dags/user_data_pipeline_automated.py`를 열어 `S3_BUCKET_NAME` 수정.
2.  Airflow UI에서 `user_data_pipeline_automated` 실행.
3.  `repair_athena_table` 태스크가 성공하면 Athena로 이동.
4.  `SELECT * FROM user_data_auto_lake;` 쿼리를 날려 데이터가 즉시 조회되는지 확인.

---

## 9. 과금방지 AWS 삭제 가이드라인
실습 종료 후 반드시 아래 순서로 자원을 삭제하세요.
1.  **S3**: 버킷 선택 -> [비우기] -> [삭제].
2.  **Glue**: Crawler 삭제 및 Database 삭제.
3.  **IAM**: 사용자(`airflow-operator`) 삭제 및 역할(`GlueS3AccessRole`) 삭제.
4.  **Docker**: 터미널에서 `docker-compose down` 실행.

---

## 10. 결론
오늘 우리는 **Airflow**를 지휘자로 사용하여 **S3(저장)**, **Glue(카탈로그)**, **Athena(분석)**를 잇는 거대한 데이터 파이프라인을 구축했습니다. 수동과 자동 실습을 통해 클라우드 데이터 레이크의 핵심 원리를 완벽히 이해하셨기를 바랍니다!
