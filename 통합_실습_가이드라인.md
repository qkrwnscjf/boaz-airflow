# 🚀 [통합 가이드] Airflow & AWS 데이터 파이프라인 실습 (입문 ➔ 실무)

본 실습은 로컬 환경에 준비된 데이터를 **"리모컨(Airflow)"**으로 **"클라우드(AWS)"**에 적재하고 분석하는 과정을 다룹니다.
API 호출 대신 로컬 데이터를 사용함으로써, 실무에서 가장 중요한 **인프라 오케스트레이션**에 집중합니다.

---

## 📅 실습 타임라인 (총 50~60분)
1. **[00~15분] 공통 환경 준비**: AWS S3/IAM 설정 및 로컬 Airflow 가동
2. **[15~35분] Track A: 수동 데이터 레이크 구축**: Glue Crawler를 이용한 데이터 카탈로그 생성 (원리 이해)
3. **[35~55분] Track B: 자동 데이터 파이프라인**: Airflow Operator를 이용한 완전 자동화 (실무 적용)
4. **[55~60분] 정리 및 질의응답**

---

## 0단계: 공통 환경 준비

### 0.1 AWS 인프라 설정
1. **S3 버킷 생성**: `boaz-data-lab-[본인이름]` (서울 리전).
2. **IAM 사용자 발급**: `airflow-operator` 이름으로 생성. (S3FullAccess, AthenaFullAccess 권한)
3. **IAM 역할(Role) 생성 (Glue용)**: `GlueS3AccessRole` 이름으로 생성. (S3FullAccess, AWSGlueServiceRole 권한)

### 0.2 로컬 Airflow 가동 및 연결
1. **터미널 실행**: `docker-compose up -d`
2. **Airflow 접속**: [http://localhost:8080](http://localhost:8080) (ID/PW: `airflow`).
3. **Connection 등록**: Admin -> Connections에서 `aws_default` 이름으로 IAM 키 등록.

---

## 1단계 [Track A]: Glue Crawler 기반 수동 실습
**목표**: 로컬 데이터를 S3에 올린 후, Glue Crawler가 어떻게 테이블 스키마를 따오는지 이해합니다.

1. **DAG 실행**: Airflow UI에서 `user_data_pipeline_manual` 활성화 및 실행.
2. **S3 확인**: `raw/user_data/data.json` 파일이 들어왔는지 확인.
3. **Glue 설정**:
   - AWS Glue 콘솔 -> **Databases** -> `boaz_database` 생성.
   - **Crawlers** -> `user_data_crawler` 생성 (S3 경로는 `raw/user_data/` 지정).
   - IAM Role은 `GlueS3AccessRole` 지정 후 **[Run]** 클릭.
4. **확인**: Athena에서 `boaz_database` 내 `user_data` 테이블 쿼리 확인.

---

## 2단계 [Track B]: 완전 자동화 파이프라인 실습
**목표**: 파티셔닝(dt=...) 구조로 데이터를 적재하고, Airflow가 Athena를 자동 갱신하는 과정을 경험합니다.

1. **Athena DDL 실행**: 자동화용 테이블 뼈대를 미리 만듭니다.
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS user_data_auto_lake (
  `id` INT, `name` STRING, `email` STRING, `company_name` STRING, `collected_at` STRING
)
PARTITIONED BY (`dt` STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://[본인버킷이름]/raw/user_data_auto/';
```
2. **DAG 실행**: Airflow UI에서 `user_data_pipeline_automated` 실행.
3. **확인**: Athena에서 `user_data_auto_lake` 테이블을 조회하여 데이터가 즉시 보이는지 확인.

---

## 📁 프로젝트 구조 정보
- `data/data.json`: 실습에 사용될 10명의 샘플 유저 데이터 (수정 불필요)
- `dags/user_data_pipeline_manual.py`: Track A용 코드
- `dags/user_data_pipeline_automated.py`: Track B용 코드
- `docker-compose.yml`: Airflow 인프라 설정 파일
