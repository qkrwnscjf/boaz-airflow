# 🚀 [통합 가이드] Airflow & AWS 데이터 파이프라인 실습 (입문 ➔ 실무)

본 실습은 **"리모컨(Airflow)"**으로 **"클라우드(AWS)"**를 조종하는 데이터 엔지니어링의 핵심을 다룹니다.
두 가지 트랙을 통해 데이터 카탈로그의 원리를 이해하고, 이를 완전 자동화하는 과정을 한 번에 체험합니다.

---

## 📅 실습 타임라인 (총 60~70분)
1. **[00~15분] 공통 환경 준비**: AWS S3/IAM 설정 및 로컬 Airflow 가동
2. **[15~40분] Track A: 수동 데이터 레이크 구축**: Glue Crawler를 이용한 데이터 카탈로그 생성 (원리 이해)
3. **[40~65분] Track B: 자동 데이터 파이프라인**: Airflow Operator를 이용한 완전 자동화 (실무 적용)
4. **[65~70분] 정리 및 질의응답**

---

## 0단계: 공통 환경 준비

### 0.1 AWS 인프라 설정
1. **S3 버킷 생성**: `boaz-data-lab-[본인이름]` (서울 리전).
2. **IAM 사용자 발급**: `airflow-operator` 이름으로 생성.
   - 권한: `AmazonS3FullAccess`, `AmazonAthenaFullAccess`
   - **Access Key / Secret Key** 메모장에 저장.
3. **IAM 역할(Role) 생성 (Glue용)**: `GlueS3AccessRole` 이름으로 생성.
   - 엔터티: `AWS 서비스` -> `Glue`
   - 권한: `AmazonS3FullAccess`, `AWSGlueServiceRole`

### 0.2 로컬 Airflow 가동 및 연결
1. **터미널 실행**: 프로젝트 루트에서 `docker-compose up -d` 실행.
2. **Airflow 접속**: [http://localhost:8080](http://localhost:8080) (ID/PW: `airflow`).
3. **Connection 등록**: Admin -> Connections에서 `aws_default` 이름으로 위에서 발급받은 IAM 키 등록.

---

## 1단계 [Track A]: Glue Crawler 기반 수동 실습
**목표**: 데이터가 S3에 저장된 후, 어떻게 '테이블'로 변환되는지 Glue Crawler의 동작 원리를 이해합니다.

1. **DAG 코드 수정**: `dags/user_data_pipeline_manual.py` 파일 내 `S3_BUCKET_NAME`을 본인 버킷으로 수정.
2. **DAG 실행**: Airflow UI에서 `user_data_pipeline_manual` 활성화 및 실행.
3. **Glue Crawler 설정**:
   - AWS Glue 콘솔 -> **Databases** -> `boaz_database` 생성.
   - **Crawlers** -> `user_data_crawler` 생성.
   - S3 경로는 `s3://[본인버킷]/raw/user_data/` 지정.
   - IAM Role은 `GlueS3AccessRole` 지정.
4. **크롤러 실행**: 생성된 크롤러를 [Run] 하고 1~2분 대기.
5. **확인**: Athena에서 `boaz_database` 내 `user_data` 테이블 쿼리 확인.

---

## 2단계 [Track B]: 완전 자동화 파이프라인 실습
**목표**: 수동으로 했던 Glue 작업을 코드로 내재화하여, 적재 즉시 분석 가능한 상태를 만드는 '자동화'를 경험합니다.

1. **Athena DDL 실행**: 자동화용 테이블 뼈대를 미리 만듭니다. (Athena 쿼리창 실행)
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS user_data_auto_lake (
  `id` INT, `name` STRING, `email` STRING, `company_name` STRING, `collected_at` STRING
)
PARTITIONED BY (`dt` STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://[본인버킷이름]/raw/user_data_auto/';
```
2. **DAG 코드 수정**: `dags/user_data_pipeline_automated.py` 내 `S3_BUCKET_NAME` 수정.
3. **DAG 실행**: Airflow UI에서 `user_data_pipeline_automated` 실행.
4. **모니터링**: `repair_athena_table` 태스크까지 모두 성공하는지 확인.
5. **확인**: Athena에서 `user_data_auto_lake` 테이블을 조회하여 데이터가 즉시 보이는지 확인.

---

## 📁 프로젝트 구조 정보
- `dags/user_data_pipeline_manual.py`: Track A용 코드 (수집/적재만 수행)
- `dags/user_data_pipeline_automated.py`: Track B용 코드 (Athena 자동 갱신 포함)
- `docker-compose.yml`: Airflow 인프라 환경 설정 파일
- `data/data.json`: 수집 API 장애 시 사용할 예비 데이터

---

## 🧹 실습 종료
- 로컬 자원 정리: `docker-compose down`
- AWS 자원 삭제: S3 버킷, IAM 사용자/역할, Glue Crawler/Database 삭제
