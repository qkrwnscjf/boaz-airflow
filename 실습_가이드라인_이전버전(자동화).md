# 🚀 실무 데이터 엔지니어링: Airflow & AWS 완전 자동화 파이프라인 (이전 버전)

본 실습에서는 로컬 Airflow를 리모컨으로 사용하여 외부 API 데이터를 수집하고, AWS S3 적재 및 Athena 데이터 카탈로그 갱신까지 **모든 과정을 Airflow 하나로 완전 자동화**하는 실습입니다.

---

## 📅 실습 타임라인 (총 40분)
1. **[00~10분]** AWS 인프라 준비 (S3 버킷 및 IAM 사용자 발급)
2. **[10~20분]** 로컬 환경 가동 및 AWS 연결 (Docker, Airflow Connection)
3. **[20~30분]** Athena 테이블 정의 (미리 스키마 만들기)
4. **[30~40분]** 파이프라인 실행(수집➔적재➔등록) 및 최종 데이터 쿼리

---

## 1단계: AWS 인프라 준비 (S3, IAM)

### 1.1 S3 버킷 생성
1. [AWS Console](https://console.aws.amazon.com/) 접속 -> **S3** 검색.
2. **[버킷 만들기]** 클릭.
3. **버킷 이름**: `boaz-user-lab-[본인이름]` (전 세계 중복 불가).
4. **리전**: `아시아 태평양(서울) ap-northeast-2` 선택 후 생성.

### 1.2 IAM 사용자(열쇠) 발급
1. **IAM** 서비스 검색 -> **사용자** -> **[사용자 생성]** 클릭.
2. **사용자 이름**: `airflow-operator` 입력.
3. **권한 설정**: [직접 정책 연결] 선택 후 아래 2개 체크.
   - `AmazonS3FullAccess`
   - `AmazonAthenaFullAccess`
4. **보안 자격 증명**: 생성된 사용자 클릭 -> [액세스 키 만들기] -> [Command Line Interface (CLI)] 선택.
5. **Access Key**와 **Secret Key**를 복사하여 메모장에 저장.

---

## 2단계: 로컬 환경 가동 및 연결

### 2.1 Docker로 Airflow 실행
터미널에서 아래 명령어를 실행합니다.
```bash
docker-compose up -d
docker ps
```

### 2.2 Airflow 접속 및 Connection 설정
1. 브라우저에서 [http://localhost:8080](http://localhost:8080) 접속. (ID/PW: `airflow`)
2. 상단 메뉴 **Admin ➔ Connections** -> **[+]** 클릭.
3. 아래 정보 입력 후 **[Save]**:
   - **Conn Id**: `aws_default`
   - **Conn Type**: `Amazon Web Services`
   - **Login**: `Access Key ID`
   - **Password**: `Secret Access Key`
   - **Extra**: `{"region_name": "ap-northeast-2"}`

---

## 3단계: Athena 테이블 정의 (DDL)

데이터를 붓기 전에 뼈대를 만듭니다.
1. AWS 콘솔에서 **Athena**로 이동. (처음 사용 시 `Settings`에서 결과 경로 지정 필요)
2. **Query Editor**에서 아래 쿼리 실행:
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS user_data_lake (
  `id` INT,
  `name` STRING,
  `email` STRING,
  `company_name` STRING,
  `collected_at` STRING
)
PARTITIONED BY (`dt` STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://[본인버킷이름]/raw/user_data/';
```

---

## 4단계: 파이프라인 가동 및 모니터링 (완전 자동화)

이전 실습과 달리 Airflow가 데이터 카탈로그 갱신까지 모두 처리합니다.

1. Airflow 메인에서 `user_data_pipeline` **[ON]** -> **[Trigger DAG]**.
2. **Graph View**에서 3개의 태스크가 초록색이 되는지 확인합니다.
   - `extract_from_api`: 데이터 수집
   - `upload_to_s3`: S3에 파티션(`dt=YYYY-MM-DD`) 구조로 적재
   - `repair_athena_table`: **[핵심]** Athena에게 새 파티션을 등록하라고 명령 (Glue 간접 제어)

---

## 5단계: 최종 데이터 확인

Airflow가 모든 작업을 끝냈으니, 우리는 결과만 보면 됩니다.
1. Athena 콘솔로 이동하여 아래 쿼리 실행.
```sql
SELECT * FROM user_data_lake LIMIT 10;
```
**에러 없이 즉시 데이터가 보인다면 완전 자동화 파이프라인 성공입니다!**

---
## 🧹 실습 종료 후 정리
```bash
docker-compose down
```
