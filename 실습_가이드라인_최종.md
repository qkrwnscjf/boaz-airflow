# 🚀 실무 데이터 엔지니어링: Airflow & AWS E2E 파이프라인 실습

본 실습에서는 로컬 Airflow를 리모컨으로 사용하여, 외부 API 데이터를 수집하고 AWS S3 적재, **AWS Glue를 통한 데이터 스키마 자동 추출**, 그리고 Athena 분석까지 이어지는 클라우드 파이프라인 전 과정을 실습합니다.

---

## 📅 실습 타임라인 (총 45분)
1. **[00~10분]** AWS 인프라 준비 (S3, IAM)
2. **[10~20분]** 로컬 Airflow 환경 가동 및 AWS 연결
3. **[20~25분]** Airflow를 통한 데이터 수집 및 S3 업로드
4. **[25~35분]** AWS Glue를 이용한 데이터 카탈로그 생성 (핵심 실습)
5. **[35~45분]** 최종 데이터 확인 (Athena)

---

## 1단계: AWS 인프라 준비 (S3, IAM)

### 1.1 S3 버킷 생성
1. [AWS Console](https://console.aws.amazon.com/) 접속 -> **S3** 검색.
2. **[버킷 만들기]** 클릭.
3. **버킷 이름**: `boaz-user-lab-[본인이름]` (전 세계 중복 불가).
4. **리전**: `아시아 태평양(서울) ap-northeast-2` 선택.
5. 나머지는 기본값으로 두고 **[버킷 만들기]** 클릭.

### 1.2 IAM 권한 발급 (Airflow용 사용자 & Glue용 역할)
**A. Airflow를 위한 사용자(열쇠) 발급**
1. **IAM** 서비스 검색 -> **사용자** -> **[사용자 생성]** 클릭.
2. **사용자 이름**: `airflow-operator` 입력.
3. **권한 설정**: [직접 정책 연결] 선택 후 `AmazonS3FullAccess` 체크.
4. **보안 자격 증명**: 생성된 사용자 클릭 -> [액세스 키 만들기] -> [Command Line Interface (CLI)] 선택.
5. **Access Key**와 **Secret Key**를 복사하여 메모장에 저장.

**B. Glue를 위한 역할(Role) 생성 (아주 중요!)**
*   *이 역할을 만들어야 Glue가 내 S3 버킷을 읽을 수 있습니다.*
1. IAM 왼쪽 메뉴에서 **역할(Roles)** -> **[역할 생성]** 클릭.
2. **신뢰할 수 있는 엔터티 유형**: `AWS 서비스` 선택.
3. **사용 사례**: `Glue` 선택 후 다음.
4. **권한 정책 연결**: 
   - `AmazonS3FullAccess` 검색 후 체크.
   - `AWSGlueServiceRole` 검색 후 체크.
5. **역할 이름**: `GlueS3AccessRole` 입력 후 생성.

---

## 2단계: 로컬 Airflow 환경 가동 및 연결

### 2.1 Docker로 Airflow 실행
터미널(Terminal)을 열고 프로젝트 폴더에서 아래 명령어를 실행합니다.
```bash
docker-compose up -d
docker ps  # 컨테이너 상태 확인
```

### 2.2 Airflow 접속 및 Connection 설정
1. 브라우저에서 [http://localhost:8080](http://localhost:8080) 접속. (ID/PW: `airflow`)
2. 상단 메뉴 **Admin ➔ Connections** -> **[+]** 클릭.
3. 아래 정보 입력 후 **[Save]**:
   - **Conn Id**: `aws_default`
   - **Conn Type**: `Amazon Web Services`
   - **Login**: 발급받은 `Access Key ID` (1.2 단계)
   - **Password**: 발급받은 `Secret Access Key` (1.2 단계)
   - **Extra**: `{"region_name": "ap-northeast-2"}`

### 2.3 DAG 코드 내 버킷 이름 수정
에디터로 `dags/user_data_pipeline.py` 파일을 열고 **11행 부근**을 수정합니다.
- `S3_BUCKET_NAME = "본인이-만든-버킷-이름"`

---

## 3단계: 파이프라인 가동 (수집 ➔ S3 적재)

1. Airflow 메인(DAGs)에서 `user_data_pipeline`을 찾습니다.
2. 파란색 스위치를 **[ON]**으로 변경 후, 오른쪽 **[Trigger DAG]** 클릭.
3. **Graph View**에서 두 개의 태스크(`extract_from_api`, `upload_to_s3`)가 `초록색`이 되는지 확인합니다.
4. **확인**: AWS S3 콘솔로 이동하여 내 버킷 안에 `raw/user_data/data.json` 파일이 들어왔는지 직접 확인합니다.

*(이전 가이드와 다르게 Airflow의 역할은 S3에 데이터를 넣는 데서 끝납니다. 이제 Glue가 나설 차례입니다!)*

---

## 4단계: AWS Glue를 이용한 데이터 카탈로그 생성 (✨핵심 실습)

이제 S3에 있는 정체불명의 JSON 파일을 Glue가 자동으로 읽어서 '테이블'로 만들어줍니다.

### 4.1 데이터베이스 생성
1. AWS 콘솔에서 **AWS Glue** 검색 후 접속.
2. 왼쪽 메뉴: **Data Catalog ➔ Databases** 클릭 -> **[Add database]** 클릭.
3. **Name**: `boaz_database` 입력 후 생성.

### 4.2 Glue 크롤러(Crawler) 생성
1. 왼쪽 메뉴: **Data Catalog ➔ Crawlers** 클릭 -> **[Create crawler]** 클릭.
2. **Name**: `user_data_crawler` 입력 후 Next.
3. **Choose data sources**: 
   - **Data source**: `S3`
   - **S3 path**: 내 버킷 안의 폴더 경로 지정 (예: `s3://[본인버킷명]/raw/user_data/`)
   - **[Add an S3 data source]** 클릭 후 Next.
4. **IAM Role**: 앞서(1.2 단계) 만든 `GlueS3AccessRole` 선택 후 Next.
5. **Output configuration**:
   - **Target database**: 방금 만든 `boaz_database` 선택 후 Next.
6. 리뷰 확인 후 **[Create crawler]** 클릭.

### 4.3 크롤러 실행
1. 생성된 `user_data_crawler`를 선택하고 상단의 **[Run]** 버튼 클릭.
2. Status가 `Running` -> `Stopping` -> `Ready`로 변할 때까지 대기합니다. (약 1~2분 소요)
3. **결과 확인**: Status 옆에 `Tables created: 1`이라고 뜨면 성공입니다! (Glue가 JSON의 구조를 스스로 파악해 냈습니다.)

---

## 5단계: 최종 데이터 확인 (Athena)

1. AWS 콘솔에서 **Athena**로 이동합니다. 
   *(처음 사용 시 `Settings`에서 결과 경로를 `s3://[본인버킷]/athena-results/`로 지정해야 합니다)*
2. 왼쪽 **Database** 목록에서 방금 전 Glue가 만든 `boaz_database`를 선택합니다.
3. 아래에 `user_data`라는 테이블이 **자동으로 생성**되어 있는 것을 확인합니다.
4. 테이블 이름 옆 점 3개 클릭 -> **[Preview Data]** 클릭 (또는 아래 쿼리 실행).

```sql
SELECT * FROM "boaz_database"."user_data" limit 10;
```

**표 형태의 유저 데이터 10줄이 보인다면 현업 표준의 AWS 데이터 파이프라인 실습 완전 성공입니다! 🎊**

---

## 🧹 실습 종료 후 정리 (과금 방지)
실습이 끝난 후 아래 명령어로 로컬 자원을 정리합니다.
```bash
docker-compose down
```
*(과금을 막기 위해 AWS S3 버킷, IAM 사용자/역할, Glue Crawler를 삭제해 주세요.)*
