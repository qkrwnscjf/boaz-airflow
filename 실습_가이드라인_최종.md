# 🚀 실무 데이터 엔지니어링: Airflow & AWS E2E 파이프라인 실습

본 실습에서는 로컬 Airflow를 리모컨으로 사용하여, 외부 API 데이터를 수집하고 AWS S3 적재 및 Athena 분석까지 이어지는 클라우드 파이프라인 전 과정을 실습합니다.

---

## 📅 실습 타임라인 (총 40분)
1. **[00~10분]** AWS 인프라 준비 (S3, IAM)
2. **[10~20분]** 로컬 환경 가동 및 AWS 연결 (Docker, Airflow Connection)
3. **[20~30분]** Athena 테이블 정의 및 코드 수정
4. **[30~40분]** 파이프라인 실행 및 최종 데이터 확인

---

## 1단계: AWS 인프라 준비 (S3, IAM)

### 1.1 S3 버킷 생성
1. [AWS Console](https://console.aws.amazon.com/) 접속 -> **S3** 검색.
2. **[버킷 만들기]** 클릭.
3. **버킷 이름**: `boaz-user-lab-[본인이름]` (전 세계 중복 불가).
4. **리전**: `아시아 태평양(서울) ap-northeast-2` 선택.
5. 나머지는 기본값으로 두고 **[버킷 만들기]** 클릭.

### 1.2 IAM 사용자(열쇠) 발급
1. **IAM** 서비스 검색 -> **사용자** -> **[사용자 생성]** 클릭.
2. **사용자 이름**: `airflow-operator` 입력.
3. **권한 설정**: [직접 정책 연결] 선택 후 아래 2개 체크.
   - `AmazonS3FullAccess`
   - `AmazonAthenaFullAccess`
4. **보안 자격 증명**: 생성된 사용자 클릭 -> [액세스 키 만들기] -> [Command Line Interface (CLI)] 선택.
5. **Access Key**와 **Secret Key**를 복사하여 메모장에 저장 (창을 닫으면 다시 볼 수 없음).

---

## 2단계: 로컬 환경 가동 및 연결

### 2.1 Docker로 Airflow 실행
터미널(Terminal)을 열고 프로젝트 폴더에서 아래 명령어를 순서대로 입력합니다.

```bash
# 1. Airflow 환경 가동 (백그라운드 실행)
docker-compose up -d

# 2. 컨테이너 상태 확인 (모든 컨테이너가 Up/Healthy일 때까지 대기)
docker ps
```

### 2.2 Airflow 접속 및 Connection 설정
1. 브라우저에서 [http://localhost:8080](http://localhost:8080) 접속.
2. **ID/PW**: `airflow` / `airflow` 입력.
3. 상단 메뉴 **Admin ➔ Connections** 클릭 -> **[+]** 버튼 클릭.
4. 아래 정보 입력 후 **[Save]**:
   - **Conn Id**: `aws_default`
   - **Conn Type**: `Amazon Web Services`
   - **Login**: 발급받은 `Access Key ID`
   - **Password**: 발급받은 `Secret Access Key`
   - **Extra**: `{"region_name": "ap-northeast-2"}`

---

## 3단계: 코드 수정 및 Athena 설정

### 3.1 DAG 코드 내 버킷 이름 수정
에디터(VS Code 등)로 `dags/user_data_pipeline.py` 파일을 엽니다.
- **11행 부근**: `S3_BUCKET_NAME = "본인이-만든-버킷-이름"` 으로 수정 후 저장.

### 3.2 Athena 테이블 정의 (DDL)
AWS 콘솔에서 **Athena**로 이동합니다. (처음 사용 시 `Settings`에서 결과 경로를 `s3://본인버킷/athena-results/`로 지정 필요)

**Query Editor**에서 아래 쿼리를 실행하여 테이블을 만듭니다.
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS user_data_lake (
  `id` INT,
  `name` STRING,
  `email` STRING,
  `company_name` STRING,
  `collected_at` STRING
)
PARTITIONED BY (`dt` STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://[본인버킷이름]/raw/user_data/';
```

---

## 4단계: 파이프라인 가동 및 모니터링

### 4.1 DAG 실행
1. Airflow 메인(DAGs)에서 `user_data_pipeline`을 찾습니다.
2. 왼쪽 파란색 스위치를 **[ON]**으로 변경.
3. 오른쪽 **[Trigger DAG]** (재생 버튼) 클릭.

### 4.2 실시간 모니터링 (중요)
1. **Graph View**: 태스크들이 `초록색`으로 변하는지 확인.
2. **Logs**: `upload_to_s3` 태스크를 클릭 -> **[Log]** 클릭.
   - `INFO - Successfully uploaded to s3://...` 메시지가 있는지 확인.

---

## 5단계: 최종 데이터 확인 (검증)

### 5.1 Athena에서 쿼리 조회
다시 AWS Athena 콘솔로 이동하여 아래 쿼리를 실행합니다.

```sql
-- 1. 데이터 인식 갱신 (Airflow가 수행했지만 확인차 다시 실행)
MSCK REPAIR TABLE user_data_lake;

-- 2. 최종 데이터 조회
SELECT * FROM user_data_lake LIMIT 10;
```

**표 형태의 유저 데이터 10줄이 보인다면 오늘 실습 성공입니다! 🎊**

---

## 🧹 실습 종료 후 정리 (과금 방지)
실습이 끝난 후 아래 명령어로 로컬 자원을 정리합니다.
```bash
docker-compose down
```
*(AWS S3 버킷과 IAM 사용자는 필요에 따라 AWS 콘솔에서 삭제하세요.)*
