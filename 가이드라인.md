# 🚀 [실전] Airflow & AWS를 활용한 클라우드 데이터 파이프라인 실습

## 0. 실습 개요
본 실습에서는 내 컴퓨터의 **Airflow**를 이용해 **AWS 클라우드**에 데이터를 적재하고 분석하는 과정을 배웁니다.
- **주제**: 로컬 JSON 데이터를 AWS S3 데이터 레이크로 이관 및 분석
- **준비**: Docker Desktop, AWS 계정

---

## 1. AWS 콘솔 접속 및 S3 버킷 생성

### 1-1. S3 버킷 생성
1. [S3 콘솔](https://s3.console.aws.amazon.com/) 접속 ➔ **[버킷 만들기]**
2. **버킷 이름**: `boaz-coin-lab-[이름]` (중복 불가)
3. **리전**: `ap-northeast-2 (서울)` 선택 후 하단의 생성 완료.

### 1-2. IAM 액세스 키 발급 (매우 중요)
1. [IAM 콘솔](https://console.aws.amazon.com/iam/) ➔ **사용자 생성** (`airflow-operator`)
2. **권한**: `AmazonS3FullAccess`, `AmazonAthenaFullAccess` 직접 연결.
3. 생성된 사용자 클릭 ➔ **보안 자격 증명** ➔ **액세스 키 만들기**.
4. 발급된 **Access Key**와 **Secret Key**를 메모장에 저장해 두세요.

---

## 2. Airflow 실행 및 AWS Connection 설정

### 2-1. Airflow 가동
```bash
docker-compose up -d
```
- [http://localhost:8080](http://localhost:8080) 접속 (ID/PW: `airflow`)

### 2-2. AWS Connection 등록 (리모컨 연결)
1. **Admin ➔ Connections** ➔ **[+]** 클릭.
2. **Conn Id**: `aws_default`
3. **Conn Type**: `Amazon Web Services`
4. **Login**: 발급받은 **Access Key**
5. **Password**: 발급받은 **Secret Key**
6. **Extra**: `{"region_name": "ap-northeast-2"}`
7. **Save** 클릭.

---

## 3. DAG 트리거 및 파이프라인 모니터링

1. **DAG 활성화**: `crypto_data_pipeline_v2` 스위치를 **ON**으로 변경.
2. **실행**: 오른쪽 재생 버튼(**Trigger DAG**) 클릭.
3. **모니터링**:
   - **Graph View**: 모든 Task가 초록색으로 변하는지 확인.
   - **Logs**: `upload_to_s3` 태스크의 로그에서 파일 전송 기록 확인.

---

## 4. AWS Athena를 이용한 데이터 쿼리 후 결과 확인

### 4-1. Athena 테이블 생성 (DDL)
Athena 쿼리 편집기에서 아래 SQL을 실행합니다. (`[본인이름]` 부분 수정 필수)

```sql
CREATE EXTERNAL TABLE IF NOT EXISTS crypto_market_data (
  bitcoin STRUCT<usd:DOUBLE, last_updated_at:BIGINT>,
  ethereum STRUCT<usd:DOUBLE, last_updated_at:BIGINT>,
  collected_at STRING
)
PARTITIONED BY (dt STRING)
ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe'
LOCATION 's3://boaz-coin-lab-[본인이름]/raw/crypto_market/';
```

### 4-2. 데이터 조회
```sql
-- 파티션 업데이트 (새 데이터 불러오기)
MSCK REPAIR TABLE crypto_market_data;

-- 결과 확인
SELECT * FROM crypto_market_data ORDER BY dt DESC;
```

---

## 💡 현업 엔지니어의 한마디
"오늘 여러분은 리모컨(Airflow)을 만들어 수천 킬로미터 떨어진 AWS라는 거대한 운동장에 데이터를 쌓았습니다. 이 구조는 실제 회사에서 사용하는 **MWAA** 환경과 100% 동일한 원리입니다."
